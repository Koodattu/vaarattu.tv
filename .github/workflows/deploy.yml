name: Deploy to Raspberry Pi 5

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Placeholder Deploy Step
        run: |
          echo "Deploying to Raspberry Pi 5..."
          # TODO: Add SSH deploy steps here
